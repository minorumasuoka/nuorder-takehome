import React, { FC } from 'react';
import './App.css';
import Autocomplete from  './Autocomplete';

const App: FC = () => {
  return (
    <div className="App">
      <Autocomplete />
    </div>
  );
};

export default App;
